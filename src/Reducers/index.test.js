import { rootReducer } from './index'

describe('rooReducer', () => {
  it('Should return an object with a each reducer', () => {

    const result = rootReducer

    expect( result ).toContain()
  })
})